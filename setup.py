from setuptools import setup

version = '0.1.0'

setup(name='samplepkg',
      version=version,
      description='A sample package used to test docs build with Sphinx and readthedocs.',
      packages=['samplepkg'])
